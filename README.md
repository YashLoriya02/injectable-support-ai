## Next Steps :-

- Replicate model and api of kbchunk (`/ingest`) in dashboard from widget
- Call `/ingest` on POST `/app` for saving md content in kb
- Change similarly in `/edit` page
- Some optimized work-around for extracting kb while generating response
- Gemini integration in response generation with concise prompt for all edge cases
- Post app creation redirect to `/app/setup` (new page) with documentation for all frontend frameworks
- Call GET `/apps/:appId` in widget (chat page) for dynamic data instead of static json
- Optimize `/search` and `/chat` APIs in widget for precise and fast response
